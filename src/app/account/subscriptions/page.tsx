import SubscriptionsClient from "./SubscriptionsClient";

export const metadata = {
  title: "Gói Đăng ký Định kỳ - Electrolux",
};

export default function SubscriptionsPage() {
  return <SubscriptionsClient />;
}
